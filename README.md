# incident-pricing

Pricing service. **Contract:** `getPrice()` returns `{ price: <number> }`.
The `incident-orders` service consumes `response.price`. Run tests: `npm test`.
Verified by the workflow-engine capstone lab on 2026-08-11.
